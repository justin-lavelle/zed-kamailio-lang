[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tree-sitter-kamailio-cfg"
description = "KamailioCfg grammar for tree-sitter"
version = "0.1.3"
keywords = ["incremental", "parsing", "tree-sitter", "kamailio-cfg"]
classifiers = [
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Topic :: Software Development :: Compilers",
  "Topic :: Text Processing :: Linguistic",
  "Typing :: Typed"
]
requires-python = ">=3.8"
license.text = "MIT"
readme = "README.md"
authors = [
  {name = "Ibrahim Shahzad", email = "ibrahim.shahzad.mirza@gmail.com"},
]
maintainers = [
  {name = "Ibrahim Shahzad", email = "ibrahim.shahzad.mirza@gmail.com"}
]

[project.urls]
Homepage = "https://github.com/tree-sitter/tree-sitter-kamailio-cfg"

[project.optional-dependencies]
core = ["tree-sitter~=0.21"]

[tool.cibuildwheel]
build = "cp38-*"
build-frontend = "build"
